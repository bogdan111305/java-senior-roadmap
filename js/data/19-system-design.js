/* Section 19: Системный дизайн */
S("Системный дизайн", "Обязательный этап почти любого senior-собеседования", [
  T("Основы масштабирования", "Базовые техники увеличения пропускной способности системы.", [
    G("Масштабирование", ["Вертикальное (мощнее один сервер) vs горизонтальное (больше серверов) масштабирование", "Балансировка нагрузки: round-robin/least-connections, L4 (транспортный уровень) vs L7 (учитывает содержимое HTTP-запроса)"]),
    G("Данные", ["Sharding — разделение данных между узлами по ключу шардирования", "Репликация: master-slave (масштабирование чтения) vs master-master (сложнее конфликты записи)"])
  ]),
  T("Согласованность данных и кэширование", "Компромиссы распределённых систем.", [
    G("Теория", ["CAP-теорема: при разделении сети (Partition tolerance обязателен) выбор между Consistency и Availability", "Eventual consistency (в итоге согласуется, выше доступность) vs strong consistency (всегда актуальные данные, ниже доступность/выше задержка)"]),
    G("Кэширование", ["Cache-aside — приложение само читает из кэша, при промахе идёт в БД и заполняет кэш", "Write-through — запись сразу и в кэш, и в БД синхронно", "Write-back — запись сначала в кэш, в БД асинхронно (риск потери данных при сбое)", "CDN — кэширование статического контента географически ближе к пользователю"])
  ]),
  T("Практика проектирования систем", "Формат, в котором обычно проходит system design интервью.", [
    G("Методология", ["Оценка нагрузки (back-of-the-envelope): DAU, RPS, объём хранимых данных, требования к задержке", "Структура ответа: уточнение требований → оценка нагрузки → high-level дизайн → углубление в проблемные компоненты → обсуждение trade-offs"]),
    G("Типовые задачи", ["Проектирование URL shortener (генерация коротких ключей, redirect, аналитика переходов)", "Проектирование ленты новостей / чата (fan-out on write vs fan-out on read)", "Проектирование rate limiter (token bucket, sliding window log/counter)"])
  ]),
  T("Резервное копирование и отказоустойчивость данных", "Часто забываемая часть system design — «есть бэкап» и «данные восстановимы» это разные утверждения.", [
    G("Метрики восстановления", ["RPO (Recovery Point Objective) — сколько данных допустимо потерять (интервал между бэкапами)", "RTO (Recovery Time Objective) — как быстро система должна вернуться в строй после сбоя", "Эти метрики — бизнес-требование, а не техническая деталь: их нужно обсуждать с заказчиком до выбора стратегии бэкапов"]),
    G("Практика", ["Полный vs инкрементальный/дифференциальный бэкап — компромисс между временем создания и временем восстановления", "«Бэкап, который ни разу не восстанавливали, не бэкап» — регулярное тестирование процедуры восстановления", "Географически распределённые копии (offsite/cross-region) как защита от отказа целого дата-центра"])
  ])
]);
