/* Section 3: Объектно-ориентированное программирование */
S("Объектно-ориентированное программирование", "Фундаментальные принципы ООП и то, как они реализованы именно в Java", [
  T("Инкапсуляция", "Сокрытие деталей реализации и защита инвариантов объекта.", [
    G("Механизмы", ["private-поля + публичные методы доступа", "Иммутабельные объекты как максимальная форма инкапсуляции", "Защитное копирование (defensive copy) для изменяемых полей (Date, коллекции)"]),
    G("Практика", ["Геттеры/сеттеры — не всегда нужны по умолчанию (анемичная модель — антипаттерн)", "Валидация инвариантов внутри конструктора/сеттера"])
  ]),
  T("Наследование", "Механизм повторного использования кода через иерархии классов.", [
    G("Механика", ["extends и обязательный/неявный вызов super() первой строкой конструктора", "Переопределение методов: сигнатура должна совпадать, возвращаемый тип — ковариантен", "Java поддерживает только одиночное наследование классов (в отличие от интерфейсов)"]),
    G("Проблемы", ["Fragile base class problem — изменение родителя ломает наследников", "Нарушение инкапсуляции при глубокой иерархии", "Вызов переопределяемого метода из конструктора родителя — источник багов"])
  ]),
  T("Полиморфизм", "Один из самых частых теоретических вопросов на собеседовании.", [
    G("Виды", ["Статический (compile-time) — overloading, выбор по типам аргументов", "Динамический (runtime) — overriding, выбор по реальному типу объекта"]),
    G("Механизм в JVM", ["Раннее связывание (static/private/final методы) vs позднее (virtual методы)", "vtable (таблица виртуальных методов) — как JVM находит нужную реализацию"])
  ], "В чём разница между overloading и overriding? Как работает динамическая диспетчеризация методов в JVM?"),
  T("Абстракция: abstract class vs interface", "Классический вопрос «когда что выбрать» на любом уровне собеседования — важно не просто перечислить отличия, а объяснить, почему они так устроены на уровне JVM.", [
    G("Abstract class: что это такое", ["Класс, который нельзя инстанцировать напрямую (new запрещён компилятором), но можно наследовать", "Может содержать: поля (в т.ч. с состоянием и значениями по умолчанию), конструктор (вызывается через super() из наследника), обычные реализованные методы и abstract-методы без тела", "Наследник обязан реализовать все abstract-методы, либо сам остаться abstract", "Модификаторы доступа полей/методов — любые (private/protected/public), в отличие от интерфейса"]),
    G("Interface: что это такое", ["Контракт поведения — набор методов, которые обязан реализовать любой implementing-класс", "До Java 8: только public abstract методы и public static final константы (неявно, без явного указания модификаторов)", "Не имеет состояния экземпляра (instance-полей) — только константы; не имеет конструктора; ссылку на интерфейс нельзя создать через new"]),
    G("Ключевое структурное отличие", ["Класс может наследовать только один abstract/обычный класс (одиночное наследование реализации) — ограничение JVM/языка против diamond problem с состоянием", "Класс может реализовывать сколько угодно интерфейсов (implements A, B, C) — так в Java эмулируется множественное наследование поведения без множественного наследования состояния", "abstract class — для «is-a» связанной иерархии с общим состоянием и частично готовой реализацией (шаблон), interface — для «can-do» контракта, не привязанного к конкретной иерархии классов"]),
    G("Интерфейсы после Java 8", ["default-методы — реализация по умолчанию прямо в интерфейсе без нарушения обратной совместимости старых реализаций при добавлении нового метода в контракт", "static-методы интерфейса — утилитные методы, привязанные к контракту, а не к конкретной реализации (например List.of(), Comparator.comparing())", "private-методы интерфейса (Java 9+) — переиспользование общего кода между несколькими default-методами без утечки его в публичный контракт", "Diamond problem при конфликте default-методов из двух интерфейсов с одинаковой сигнатурой — компилятор требует явного переопределения метода в implementing-классе (неоднозначность нельзя оставить неразрешённой)"]),
    G("Функциональные интерфейсы", ["Интерфейс ровно с одним abstract-методом (SAM — Single Abstract Method); default/static-методы не считаются", "@FunctionalInterface — необязательная, но полезная аннотация: заставляет компилятор проверить, что интерфейс действительно функциональный", "Именно к функциональным интерфейсам можно присвоить лямбда-выражение или method reference — компилятор неявно генерирует реализацию через invokedynamic/LambdaMetafactory (см. раздел JVM)"]),
    G("Sealed-иерархии (Java 17)", ["sealed interface / sealed class с permits — явно ограничивает список классов, которые могут реализовать/унаследовать тип", "non-sealed — «открывает» иерархию обратно для конкретного наследника", "Ценность: компилятор проверяет исчерпывающность switch-выражения по sealed-иерархии без default-ветки — невозможно забыть обработать новый вариант"]),
    G("Под капотом в JVM", ["Вызов метода интерфейса компилируется в инструкцию байткода invokeinterface, обычного/абстрактного класса — в invokevirtual", "invokevirtual использует vtable (таблицу виртуальных методов) конкретного класса — поиск за O(1) по фиксированному индексу", "invokeinterface исторически медленнее: JVM ищет реализацию через itable (таблица для каждого интерфейса) — у объекта может быть несколько itable, по одной на реализуемый интерфейс; современный JIT почти полностью убирает эту разницу через инлайн-кэши и спекулятивную оптимизацию при мономорфных вызовах"])
  ], "Почему в Java нельзя наследовать два класса, но можно реализовывать много интерфейсов? Что изменится в байткоде, если метод объявлен в интерфейсе, а не в abstract-классе? Когда выбрать abstract class, а когда interface, если оба варианта технически подходят?"),
  T("Композиция vs наследование", "Принцип, который отличает крепкий дизайн от хрупкого.", [
    G("Принцип", ["«Предпочитайте композицию наследованию» (Effective Java)", "has-a (композиция) vs is-a (наследование) отношения", "Делегирование вызовов вложенному объекту вместо переопределения методов родителя"])
  ]),
  T("Класс Object — универсальный суперкласс", "Каждый класс в Java неявно наследует Object, даже если extends не написан явно — важно знать, что именно достаётся «бесплатно» и почему часть этого наследия сегодня считается ошибкой дизайна.", [
    G("Место в иерархии", ["Если класс не указывает extends, компилятор неявно подставляет extends Object — единственный класс без родителя (getClass().getSuperclass() у Object возвращает null)", "Массивы в Java тоже являются объектами и неявно наследуют Object (можно вызвать hashCode()/toString() на массиве, хоть и без особого смысла)"]),
    G("getClass(), equals(), hashCode(), toString()", ["getClass() — final-метод, возвращает Class<?> реального типа объекта в runtime (полезно для проверки типа надёжнее, чем instanceof, когда важна точность класса, а не иерархии)", "Реализация equals() по умолчанию — сравнение ссылок (==), т.е. идентичность объекта, а не логическое равенство", "Реализация hashCode() по умолчанию — обычно основана на identity hash code (не гарантированно адрес в памяти, JVM-специфично), уникальна для каждого объекта до переопределения", "toString() по умолчанию возвращает getClass().getName() + \"@\" + Integer.toHexString(hashCode()) — та самая строка вида com.example.Foo@1b6d3586"]),
    G("clone() — зачем и почему считается неудачным API", ["protected native Object clone() — побитовая (shallow) копия полей объекта; для работы обязателен маркерный интерфейс Cloneable, иначе CloneNotSupportedException", "Shallow copy — вложенные изменяемые объекты (например массивы, коллекции) не копируются рекурсивно, копия и оригинал делят одни и те же ссылки", "Effective Java (Joshua Bloch) прямо называет clone()/Cloneable «сломанным» механизмом — на практике предпочитают copy-конструктор, статическую factory-копию или билдер"]),
    G("wait()/notify()/notifyAll()", ["native-методы, работающие с intrinsic-монитором объекта — тем же, что захватывает synchronized", "Обязаны вызываться из кода, уже удерживающего монитор этого объекта, иначе IllegalMonitorStateException", "Подробный механизм и типичные ошибки использования разобраны в разделе про многопоточность"]),
    G("finalize() — устаревший и по факту не используемый механизм", ["Метод, который GC мог вызвать перед освобождением памяти объекта — момент вызова непредсказуем, JVM не гарантирует, что он вообще будет вызван", "Deprecated начиная с Java 9 из-за непредсказуемости, влияния на производительность GC и риска «воскрешения» объекта из finalize()", "Замена: try-with-resources + AutoCloseable для детерминированного освобождения ресурсов, java.lang.ref.Cleaner — для redundant-подстраховки на случай, если close() забыли вызвать"])
  ], "Что произойдёт, если вызвать equals()/hashCode() без их переопределения — как они себя поведут по умолчанию? Почему clone() считается неудачным решением в API Java? Чем финализация отличается от try-with-resources?"),
  T("Контракт equals/hashCode/toString", "Нарушение контракта — классическая причина трудноуловимых багов.", [
    G("Контракт equals", ["Рефлексивность: x.equals(x) == true", "Симметричность: x.equals(y) == y.equals(x)", "Транзитивность и согласованность с hashCode"]),
    G("Почему hashCode обязателен", ["Равные объекты обязаны иметь одинаковый hashCode", "Нарушение контракта → объект «теряется» в HashMap/HashSet", "IDE-генерация vs Lombok @EqualsAndHashCode vs record (генерирует автоматически)"])
  ], "Почему если переопределён equals, обязательно нужно переопределить hashCode? Что сломается, если этого не сделать?"),
  T("Comparable и Comparator", "Два способа задать порядок сортировки объектов.", [
    G("Comparable", ["Метод compareTo(T o), «естественный порядок» объекта", "Контракт: знак результата, согласованность с equals (желательно)"]),
    G("Comparator", ["Внешний, не завязанный на класс порядок сортировки", "Comparator.comparing(keyExtractor).thenComparing(...).reversed()", "Collections.sort / List.sort / Stream.sorted с Comparator"])
  ]),
  T("Вложенные и анонимные классы", "Разные способы организации кода внутри классов.", [
    G("Виды", ["Static nested class — не хранит ссылку на внешний объект", "Inner (non-static) class — хранит неявную ссылку Outer.this, требует экземпляр внешнего класса", "Local class — объявлен внутри метода", "Anonymous class — безымянный, создаётся и используется один раз"]),
    G("Современная альтернатива", ["Лямбды заменяют анонимные классы для функциональных интерфейсов (компактнее, без this-ловушек)"])
  ])
]);
