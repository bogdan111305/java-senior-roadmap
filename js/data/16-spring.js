/* Section 16: Spring Framework и Spring Boot */
S("Spring Framework и Spring Boot", "Стандарт де-факто в корпоративной Java-разработке — разобран по каждому модулю отдельно", [
  T("IoC-контейнер", "Сердце Spring — управление жизненным циклом бинов.", [
    G("Контейнер", ["BeanFactory — базовый контейнер (ленивая инициализация) vs ApplicationContext — расширенный (события, i18n, ранняя инициализация singleton-бинов)", "Bean definition — метаданные о том, как создать и сконфигурировать бин"]),
    G("Жизненный цикл бина", ["Создание экземпляра → внедрение зависимостей → @PostConstruct/InitializingBean → использование → @PreDestroy/DisposableBean", "BeanPostProcessor — хук до/после инициализации любого бина, BeanFactoryPostProcessor — модификация bean definitions до создания экземпляров", "Scopes: singleton (по умолчанию), prototype, request, session, application"]),
    G("Стереотипы", ["@Component — базовая аннотация-стереотип, @Service/@Repository/@Controller — семантические специализации", "@Repository дополнительно транслирует специфичные исключения БД в DataAccessException"]),
    G("Под капотом: как именно создаётся бин", ["getBean() → doCreateBean(): 1) createBeanInstance — выбор конструктора (если их несколько, Spring ищет @Autowired-конструктор или единственный конструктор) и создание «голого» объекта через рефлексию", "2) populateBean — внедрение зависимостей в поля/сеттеры (DI происходит после создания объекта, а не во время конструирования, если это не constructor injection)", "3) initializeBean — по порядку: Aware-интерфейсы (BeanNameAware/ApplicationContextAware), postProcessBeforeInitialization всех BeanPostProcessor'ов, затем @PostConstruct/afterPropertiesSet, затем postProcessAfterInitialization", "Именно на шаге postProcessAfterInitialization AOP-прокси (AbstractAutoProxyCreator, сам являющийся BeanPostProcessor) оборачивает готовый бин — поэтому из контейнера в итоге достаётся не «голый» объект, а прокси над ним"])
  ]),
  T("Dependency Injection в Spring", "Практическая реализация принципа DI.", [
    G("Способы внедрения", ["Constructor injection — рекомендуемый способ: позволяет final-поля, делает обязательные зависимости явными, упрощает unit-тестирование без контейнера", "Field injection (@Autowired на поле) — компактно, но усложняет тестирование и скрывает обязательные зависимости", "Setter injection — для опциональных/переконфигурируемых зависимостей"]),
    G("Разрешение неоднозначности", ["@Qualifier — выбор конкретного бина при нескольких кандидатах одного типа", "@Primary — бин-кандидат по умолчанию среди нескольких реализаций", "Циклические зависимости между бинами — ошибка при constructor injection, «решается» (с оговорками) через @Lazy или field injection, но чаще сигнализирует о проблеме дизайна"]),
    G("Под капотом: алгоритм @Autowired", ["Сначала поиск кандидатов по типу (byType); если найден ровно один — используется он", "Если кандидатов несколько — Spring ищет бин с @Primary; если его нет, сверяет имя поля/параметра с именем бина (byName) или требует явный @Qualifier", "Ни один подходящий кандидат не найден и нет required=false/Optional/@Nullable → NoSuchBeanDefinitionException при старте контекста"]),
    G("Под капотом: как разрешаются циклы (three-level cache)", ["Только для singleton-бинов с setter/field injection — Spring держит три кэша: singletonObjects (готовые бины), earlySingletonObjects (незавершённые, но уже созданные экземпляры), singletonFactories (фабрики для создания «ранней» ссылки)", "Когда бин A на этапе populateBean нуждается в B, а B нуждается в A — контейнер отдаёт A из singletonFactories как «раннюю» (ещё не до конца проинициализированную) ссылку, чтобы завершить создание B, а затем вернуться и доинициализировать A", "Именно поэтому constructor injection не может разрешить цикл: объект должен полностью существовать до вызова конструктора — «ранней» ссылки для него ещё не существует, только BeanCurrentlyInCreationException"])
  ]),
  T("Spring Boot: автоконфигурация и запуск приложения", "Слой автоконфигурации поверх Spring Framework.", [
    G("Автоконфигурация", ["@SpringBootApplication = @Configuration + @EnableAutoConfiguration + @ComponentScan", "Автоконфигурации подключаются условно через @Conditional (@ConditionalOnClass, @ConditionalOnMissingBean, @ConditionalOnProperty)", "spring.factories / META-INF/spring/…AutoConfiguration.imports — механизм регистрации автоконфигураций"]),
    G("Конфигурация приложения", ["Starters — наборы транзитивных зависимостей под конкретную задачу (spring-boot-starter-web, -data-jpa, -security)", "application.yml/properties, профили (spring.profiles.active) для разных окружений", "@ConfigurationProperties — типобезопасное связывание конфигурации с Java-объектом (в отличие от точечных @Value)"]),
    G("Под капотом: почему автоконфигурация не мешает своим бинам", ["@ConditionalOnMissingBean — автоконфигурация Spring Boot регистрирует бин по умолчанию, только если разработчик не объявил свой бин того же типа — поэтому «магия» никогда не переопределяет явную конфигурацию", "Условия проверяются в порядке @AutoConfigureOrder/@AutoConfigureBefore/@AutoConfigureAfter — некоторые автоконфигурации обязаны выполниться раньше других (например DataSource раньше JPA)", "--debug при запуске выводит Condition Evaluation Report — какие автоконфигурации применились, а какие нет и почему (полезно для диагностики «почему бин не создался»)", "spring.autoconfigure.exclude — явное отключение конкретной автоконфигурации, если нужно заменить её своей"]),
    G("Альтернативы Spring Boot", ["Quarkus — «Kubernetes-native» фреймворк: обработка аннотаций и DI-графа переносится на этап сборки (build-time), а не в рантайм — за счёт этого радикально быстрый старт и низкое потребление памяти, особенно в связке с GraalVM native image", "Micronaut — похожая идея (compile-time DI и AOP вместо рефлексии в рантайме), тоже нацелен на serverless/контейнерные сценарии с холодным стартом", "Компромисс: Spring Boot — самая большая экосистема и community-поддержка, Quarkus/Micronaut — быстрее стартуют и легче по памяти, но меньше готовых интеграций и людей на рынке труда"])
  ]),
  T("Spring MVC / Web", "Обработка HTTP-запросов.", [
    G("Обработка запроса", ["DispatcherServlet — единая точка входа, делегирует HandlerMapping/HandlerAdapter", "@RestController = @Controller + @ResponseBody, @RequestMapping и производные (@GetMapping и т.д.)", "Filter (уровень сервлет-контейнера, до Spring) vs Interceptor (HandlerInterceptor, внутри Spring MVC, знает о хендлере)"]),
    G("Ошибки и валидация", ["@ControllerAdvice + @ExceptionHandler — централизованная обработка исключений по всему приложению", "Bean Validation (JSR-380): @Valid/@Validated, @NotNull/@Size/@Pattern, кастомные валидаторы через ConstraintValidator", "ResponseEntityExceptionHandler как базовый класс для типовых ошибок Spring MVC"]),
    G("Под капотом: полный путь запроса", ["Запрос сначала проходит цепочку сервлет-фильтров контейнера (Tomcat), затем попадает в единственный сервлет — DispatcherServlet (front controller pattern)", "DispatcherServlet перебирает HandlerMapping'и, чтобы найти подходящий метод-обработчик по URL/методу/заголовкам (RequestMappingHandlerMapping разбирает @RequestMapping-аннотации)", "Найденный обработчик оборачивается в HandlerExecutionChain (сам хендлер + применимые HandlerInterceptor'ы) и передаётся в HandlerAdapter, который умеет вызывать конкретный тип хендлера", "Аргументы метода-контроллера заполняются через HandlerMethodArgumentResolver (например @RequestBody резолвится через HttpMessageConverter, десериализующий JSON в объект через Jackson)", "Возвращаемое значение для @ResponseBody сериализуется обратно тем же HttpMessageConverter согласно заголовку Accept — отсюда и «магическое» превращение объекта в JSON без ручного кода"])
  ]),
  T("Spring Data JPA: репозитории и запросы", "Сокращение boilerplate-кода для работы с БД поверх JPA (см. также раздел про Hibernate/JPA).", [
    G("Репозитории", ["JpaRepository/CrudRepository/PagingAndSortingRepository — готовые CRUD-операции без реализации", "Query methods по имени метода — Spring парсит имя и строит запрос"]),
    G("Динамические запросы", ["Specifications — композиция условий (and/or) поверх Criteria API", "Projections (интерфейсные/DTO) — выборка только нужных полей без загрузки всей сущности", "Auditing: @CreatedDate/@LastModifiedDate/@CreatedBy через @EnableJpaAuditing"]),
    G("Под капотом: как работает интерфейс без единой строчки кода", ["Разработчик объявляет только интерфейс (extends JpaRepository) — реальная реализация создаётся на старте контекста как JDK dynamic proxy (или через SimpleJpaRepository + прокси-обёртку) — implementing-класса в исходниках нет", "Каждый вызов метода перехватывается RepositoryFactoryBeanSupport/QueryExecutorMethodInterceptor, который решает, какой запрос выполнить", "Query methods по имени: PartTree разбирает имя метода (findByLastNameAndAgeGreaterThan) на дерево условий (Part) ещё на старте приложения — если имя не разбирается по правилам, ошибка возникает сразу при запуске контекста, а не в рантайме на первом вызове", "Методы, унаследованные напрямую от JpaRepository (save/findById/deleteById), делегируются в SimpleJpaRepository — готовую реализацию Spring Data, а не генерируются заново"])
  ]),
  T("Spring Security", "Аутентификация и авторизация в Spring-приложениях.", [
    G("Архитектура", ["SecurityFilterChain — цепочка сервлет-фильтров (аутентификация, авторизация, CSRF, CORS)", "AuthenticationManager/AuthenticationProvider — проверка учётных данных", "UserDetailsService — загрузка данных пользователя, PasswordEncoder (BCrypt) — хранение и сверка паролей"]),
    G("Авторизация и токены", ["Method security: @PreAuthorize/@PostAuthorize/@Secured на уровне методов сервисов", "JWT-аутентификация: свой фильтр, извлекающий и валидирующий токен из заголовка Authorization", "OAuth2/OpenID Connect: spring-security-oauth2-client (роль client) и resource-server (валидация токенов от внешнего Authorization Server)"]),
    G("Под капотом: FilterChainProxy и SecurityContext", ["Spring Security регистрируется в сервлет-контейнере одним фильтром — FilterChainProxy, который внутри последовательно вызывает набор своих внутренних фильтров (это отдельная, независимая от обычных сервлет-фильтров цепочка)", "Типовой порядок: фильтр логирования → CorsFilter → CsrfFilter → фильтр аутентификации (например UsernamePasswordAuthenticationFilter или свой JWT-фильтр) → ExceptionTranslationFilter (перехватывает AccessDeniedException/AuthenticationException) → FilterSecurityInterceptor/AuthorizationFilter (принятие решения о доступе)", "SecurityContextHolder — по умолчанию ThreadLocal-хранилище текущего Authentication (в реактивном стеке — через Reactor Context, а не ThreadLocal, потому что нет привязки к одному потоку)", "Authentication — объект с principal (кто), credentials (обычно очищаются после аутентификации) и коллекцией GrantedAuthority (что можно)"])
  ]),
  T("Spring AOP", "Сквозная функциональность без загрязнения бизнес-логики.", [
    G("Механизм", ["Proxy-based AOP: JDK dynamic proxy (если есть интерфейс) vs CGLIB (наследование класса, если интерфейса нет)", "Advice-типы: @Before, @After, @AfterReturning, @AfterThrowing, @Around (полный контроль над вызовом через ProceedingJoinPoint)", "Pointcut-выражения: execution(* com.example.service.*.*(..))"]),
    G("Ограничения", ["Self-invocation problem — вызов аннотированного метода из другого метода того же класса не проходит через прокси (advice не срабатывает)", "Типичные применения: логирование, @Transactional, @Cacheable, аудит"]),
    G("Под капотом: откуда берётся прокси и как строится цепочка advice", ["AbstractAutoProxyCreator (реализация BeanPostProcessor) на этапе postProcessAfterInitialization решает, подходит ли бин под какой-либо pointcut, и если да — оборачивает его в прокси", "Если у бина есть интерфейс — по умолчанию JDK Dynamic Proxy (java.lang.reflect.Proxy, реализует те же интерфейсы); если интерфейса нет или явно указан proxyTargetClass=true — CGLIB (динамический подкласс через генерацию байткода, поэтому final-классы/методы нельзя проксировать через CGLIB)", "Несколько advice на одном методе выстраиваются в единую цепочку интерсепторов (аналог Servlet Filter Chain) — каждый advice вызывает proceed() для передачи управления следующему звену, самим целевым методом цепочка заканчивается", "Именно поэтому self-invocation не работает: вызов this.method() идёт напрямую на «голый» объект в обход прокси, через который единственно и запускается цепочка advice"])
  ]),
  T("Транзакции в Spring: @Transactional изнутри", "Одна из самых частых тем «а что будет, если...» на собеседованиях среднего и старшего уровня — построена на том же proxy-механизме, что и Spring AOP.", [
    G("Механизм", ["@Transactional — это тоже AOP advice: TransactionInterceptor оборачивает вызов метода в try/commit/catch/rollback через PlatformTransactionManager", "Именно поэтому self-invocation ломает и @Transactional — вызов аннотированного метода из того же класса идёт мимо прокси, транзакция не начинается", "Транзакция физически привязана к соединению/Hibernate-сессии через ThreadLocal (TransactionSynchronizationManager) — весь код в пределах вызова работает с одним и тем же соединением"]),
    G("Propagation (распространение)", ["REQUIRED (по умолчанию) — использует текущую транзакцию, если есть, иначе создаёт новую", "REQUIRES_NEW — всегда создаёт новую независимую транзакцию, приостанавливая текущую (нужен отдельный физический connection)", "NESTED — вложенная транзакция через savepoint внутри той же физической транзакции (откат только до savepoint)", "MANDATORY/SUPPORTS/NOT_SUPPORTED/NEVER — более редкие стратегии для явного контроля обязательности транзакции"]),
    G("Откат и типичные ошибки", ["По умолчанию rollback происходит только на unchecked-исключениях (RuntimeException/Error), checked-исключение транзакцию НЕ откатывает, если явно не указано rollbackFor", "Isolation level (READ_COMMITTED/REPEATABLE_READ/SERIALIZABLE) — управляет тем, какие аномалии параллельных транзакций допустимы (см. раздел про БД)", "@Transactional на private-методе или на методе, вызванном рефлексией без прокси, — молча не работает, без явной ошибки на этапе компиляции", "readOnly=true — подсказка Hibernate/JDBC-драйверу для оптимизации (например отключение dirty checking), не является жёсткой гарантией на уровне БД"])
  ], "Что произойдёт, если публичный метод с @Transactional вызвать из другого метода того же класса? Почему checked-исключение по умолчанию не откатывает транзакцию? Чем REQUIRES_NEW отличается от NESTED?"),
  T("Spring Cloud и микросервисы", "Инструментарий для распределённых Spring-приложений.", [
    G("Инфраструктурные компоненты", ["Config Server — централизованное хранение конфигурации для множества сервисов", "Service discovery: Eureka / Consul — сервисы регистрируются и находят друг друга по имени, а не фиксированному адресу", "API Gateway: Spring Cloud Gateway — единая точка входа, маршрутизация, rate limiting"]),
    G("Отказоустойчивость и вызовы между сервисами", ["Feign — декларативный HTTP-клиент между сервисами", "Балансировка нагрузки на стороне клиента (Spring Cloud LoadBalancer)", "Resilience4j — Spring-реализация Circuit Breaker/Bulkhead/Retry (сам паттерн и его состояния разобраны в разделе про микросервисную архитектуру)"])
  ]),
  T("Реактивный стек: Spring WebFlux", "Неблокирующая альтернатива Spring MVC для высоконагруженных IO-bound сценариев.", [
    G("Основы Reactor", ["Mono<T> (0..1 элемент) и Flux<T> (0..N элементов) — реактивные типы Project Reactor", "Подписка (subscribe) как момент запуска ленивого потока данных", "Операторы: map/flatMap/filter/zip/merge, аналогичны Stream API, но асинхронны"]),
    G("Практика", ["Backpressure — потребитель управляет скоростью, с которой издатель присылает данные", "R2DBC — реактивный доступ к реляционным БД (в отличие от блокирующего JDBC)", "Когда WebFlux оправдан (много параллельных IO-bound соединений), а когда усложняет код без выгоды"])
  ]),
  T("Тестирование Spring-приложений", "Специфика тестирования приложений на фреймворке.", [
    G("Срезы тестирования", ["@SpringBootTest — полный контекст (медленнее, ближе к реальности)", "@WebMvcTest — только web-слой с MockMvc, зависимости мокируются через @MockBean", "@DataJpaTest — только JPA-слой с автоконфигурируемой in-memory или Testcontainers БД"]),
    G("Инструменты", ["MockMvc — отправка тестовых HTTP-запросов без реального сервера", "Тестовые профили (application-test.yml) и Testcontainers для интеграционных тестов, близких к продакшену"])
  ]),
  T("Spring Boot Actuator и production-ready функции", "То, что делает Spring Boot приложение наблюдаемым в проде.", [
    G("Возможности", ["/actuator/health, /actuator/metrics, /actuator/info — встроенные эндпоинты состояния", "Micrometer — фасад метрик, экспортируемых в Prometheus/Grafana/другие системы", "Кастомные HealthIndicator для проверки состояния внешних зависимостей (БД, брокер сообщений)"])
  ]),
  T("Планирование задач: @Scheduled и Quartz", "Фоновые и периодические задачи — то, что обычно недооценивают, пока не столкнутся с дублирующимся выполнением на нескольких инстансах приложения.", [
    G("Spring @Scheduled", ["@EnableScheduling + @Scheduled(fixedRate/fixedDelay/cron=...) — простейший способ запускать метод по расписанию внутри Spring-приложения", "fixedRate (следующий запуск строго через интервал от начала предыдущего) vs fixedDelay (интервал от окончания предыдущего запуска) — частая путаница", "По умолчанию все @Scheduled-методы выполняются в ОДНОМ потоке последовательно — долгая задача блокирует остальные; нужен свой TaskScheduler/пул потоков для параллельного выполнения"]),
    G("Проблема нескольких инстансов", ["При горизontальном масштабировании (несколько подов/инстансов) @Scheduled сработает на КАЖДОМ инстансе одновременно — если задача не идемпотентна, это дублирует работу или портит данные", "ShedLock — библиотека распределённой блокировки поверх БД/Redis специально для этого случая: гарантирует, что задача выполнится только на одном инстансе за раз"]),
    G("Quartz Scheduler", ["Полноценный планировщик с персистентным хранением задач (JobStore в БД) — расписание переживает перезапуск приложения, в отличие от @Scheduled", "Job (что делать) и Trigger (когда делать) разделены — одну Job можно запускать по нескольким разным триггерам", "Кластерный режим Quartz из коробки решает ту же проблему дублирования на нескольких инстансах, что ShedLock решает для @Scheduled"])
  ], "Чем fixedRate отличается от fixedDelay? Что произойдёт с @Scheduled-задачей при масштабировании приложения на 3 инстанса?")
]);
